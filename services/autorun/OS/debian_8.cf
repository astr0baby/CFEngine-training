
bundle agent debian_8 {

vars:

  "bundles[debian_common]"             string => "debian_common.cf";

  "sequence"  slist => getindices("bundles");
  "inputs"    slist => getvalues("bundles");

methods:

 "any" usebundle => debian_common;

}
